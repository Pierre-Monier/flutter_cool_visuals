name: flutter_canvart
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  fast_noise: ^1.0.1
  flutter:
    sdk: flutter
  url_launcher: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  lint: ^1.10.0

flutter:
  uses-material-design: true
  fonts:
    - family: Remix Icon
      fonts:
        - asset: asset/remixicon.ttf
