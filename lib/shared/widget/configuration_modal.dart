import 'package:flutter/material.dart';

class ConfigurationModal extends StatelessWidget {
  const ConfigurationModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}
